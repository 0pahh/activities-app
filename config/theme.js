import { MD3LightTheme as DefaultTheme } from "react-native-paper"

const theme = {
    ...DefaultTheme,
    myOwnProperty: true,
    colors: {
      primary:  "#0077B6",
      blue:       "#00B4D8",
      lightblue:  "#90E0EF",
      cyan:       "#CAF0F8",
      background: "#fff",
      whiteTxt: "#fff"
    }
  };

export default theme;